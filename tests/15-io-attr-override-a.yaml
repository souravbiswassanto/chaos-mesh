apiVersion: chaos-mesh.org/v1alpha1
kind: IOChaos
metadata:
  name: pg-io-attr-override-replica
  namespace: chaos-mesh
spec:
  action: attrOverride
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
      "kubedb.com/role": "standby"
  volumePath: /var/pv
  path: /var/pv/data/**/*
  attr:
    perm: 444
  percent: 100
  duration: "5m"
  containerNames:
    - postgres
