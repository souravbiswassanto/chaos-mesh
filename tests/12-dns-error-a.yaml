apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata:
  name: pg-replica-dns-error
  namespace: chaos-mesh
spec:
  action: error
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
      "kubedb.com/role": "standby"
  duration: "5m"
