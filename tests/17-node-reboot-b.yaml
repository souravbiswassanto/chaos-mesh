apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pg-node-reboot-single
  namespace: chaos-mesh
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
  gracePeriod: 0
  duration: "30s"
