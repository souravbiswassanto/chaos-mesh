apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pg-all-pods-pod-failure
  namespace: chaos-mesh
spec:
  action: pod-failure
  mode: all
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
  duration: "2m"
