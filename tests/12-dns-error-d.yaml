apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata:
  name: pg-dns-error-short
  namespace: chaos-mesh
spec:
  action: error
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
      "kubedb.com/role": "primary"
  duration: "2m"
