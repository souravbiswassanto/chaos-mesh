apiVersion: chaos-mesh.org/v1alpha1
kind: TimeChaos
metadata:
  name: pg-time-offset-small
  namespace: chaos-mesh
spec:
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
      "kubedb.com/role": "primary"
  timeOffset: "-30m"
  clockIds:
    - CLOCK_REALTIME
  duration: "5m"
