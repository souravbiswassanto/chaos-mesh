apiVersion: chaos-mesh.org/v1alpha1
kind: DNSChaos
metadata:
  name: pg-dns-error-all
  namespace: chaos-mesh
spec:
  action: error
  mode: all
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
  duration: "3m"
