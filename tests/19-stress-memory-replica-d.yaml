apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: pg-memory-stress-light
  namespace: chaos-mesh
spec:
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
      "kubedb.com/role": "standby"
  stressors:
    memory:
      workers: 1
      size: "500MB"
  duration: "5m"
