apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: pg-all-oom-light
  namespace: chaos-mesh
spec:
  mode: all
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
  stressors:
    memory:
      workers: 1
      size: "800MB"
  duration: "5m"
