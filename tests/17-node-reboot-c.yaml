apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: pg-node-reboot-primary
  namespace: chaos-mesh
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - demo
    labelSelectors:
      "app.kubernetes.io/instance": "pg-ha-cluster"
      "kubedb.com/role": "primary"
  gracePeriod: 0
  duration: "30s"
